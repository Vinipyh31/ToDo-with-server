import React from 'react';

const TodoList = () => {
    return (
        <div>
            Мапимся по списку и передаем обькт ТоДо в компонент TodoItem
        </div>
    );
};

export default TodoList;